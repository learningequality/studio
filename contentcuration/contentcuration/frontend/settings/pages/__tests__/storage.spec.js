// import { mount } from '@vue/test-utils';
// import Storage from '../Storage/index';

// function makeWrapper() {
//   return mount(Storage, {
//     computed: {
//       session() {
//         return {
//           currentUser: {
//             disk_space: 300,
//             available_space: 100,
//           },
//         };
//       },
//     },
//   });
// }

describe('storage tab', () => {
  it('clicking toggle link should show request form', () => {
    // TODO: add KThemePlugin to jest_config/setup (getting syntax error)
    // let wrapper = makeWrapper();
    // wrapper.find('[data-test="toggle-link"]').trigger('click');
    // expect(wrapper.vm.showRequestForm).toBe(true);
  });
});
