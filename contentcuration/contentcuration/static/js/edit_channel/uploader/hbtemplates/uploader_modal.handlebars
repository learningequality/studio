<div class="modal fade" id="uploaderModal"  tabindex="-1">
  <div class="modal-dialog">
    <div class="modal-content upload-modal-content">
      <div class="modal-header">
        <button type="button" class="close" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">&times;</span></button>
        <h4 class="modal-title truncate">
          {{#if new_content}}
            <i class="material-icons align-text-top">add</i>
            &nbsp; {{#if new_topic}}{{formatMessage (intlGet 'messages.add_topics')}}{{else}}{{formatMessage (intlGet 'messages.add_exercise')}}{{/if}}
          {{else}}
            {{#if allow_edit}}
              <i class="material-icons align-text-top">mode_edit</i>
              &nbsp; {{#if node}}{{formatMessage (intlGet 'messages.editing_kind') kind=(translate node.kind)}}{{else}}{{formatMessage (intlGet 'messages.editing_content')}}{{/if}}
            {{else}}
              <i class="material-icons align-text-top">pageview</i>
              &nbsp; {{#if node}}{{formatMessage (intlGet 'messages.viewing_kind') kind=(translate node.kind)}}{{else}}{{formatMessage (intlGet 'messages.viewing_content')}}{{/if}}
            {{/if}}
          {{/if}}
        </h4>
      </div>
      <div class="modal-body">
      </div>
    </div>
  </div>
</div>
