{{#each tags}}
	<div class="col-xs-4 tag" value="tag_name">
		{{this}}
		<span class='glyphicon glyphicon-remove pull-right delete_tag' aria-hidden='true' value='{{this}}' title="{{formatMessage (intlGet 'messages.remove_tag')}}"></span>
	</div>
{{/each}}